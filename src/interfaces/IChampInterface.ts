export interface IChampListApi {
  id: string;
  name: string;
  user_id: string;
  picture_url: string;
  is_favorite: boolean;
  style_id: string;
}

export interface IChampList {
  id: string;
  name: string;
  user_id: string;
  pictureUrl: string;
  isFavorite: boolean;
  styleId: string;
}
